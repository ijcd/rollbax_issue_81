defmodule RollbaxIssue81Web.LayoutViewTest do
  use RollbaxIssue81Web.ConnCase, async: true

  # TODO
  # test "Sign in link shown when not signed in", %{conn: conn} do
  #   conn = get conn, "/"
  #
  #   assert html_response(conn, 200) =~ "Login"
  # end
  
  # TODO
  # test "Sign out link shown when signed in", %{conn: conn} do
  #   user = insert(:user)
  #
  #   conn = conn
  #   |> assign(:user, user)
  #   |> get("/")
  # 
  #   assert html_response(conn, 200) =~ "Sign out"
  # end  
end
